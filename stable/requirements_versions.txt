# 💡 Очищено от конфликтов и дубликатов

accelerate==0.21.0
einops==0.4.1
# fastapi==0.94.0
gfpgan==1.3.8
gradio==3.32.0
inflection==0.5.1
jsonmerge==1.8.0
kornia==0.6.7
lark==1.1.2
numpy==1.23.5
omegaconf==2.2.3
opencv-python==4.7.0.68
safetensors==0.3.1
scipy==1.10.1
torch==2.0.1
uvicorn==0.20.0
xformers==0.0.20
# transformers==4.29.2
# diffusers==0.11.1
torchsde==0.2.6
httpx==0.24.1
httpcore==0.16.3
# bitsandbytes — УДАЛЁН, так как вызывает ошибку без GPU
# pydantic — НЕ УКАЗЫВАЕМ, так как fastapi требует <2.0, а transformers требует >=2.0
# !pip uninstall -y jax jaxlib flax bitsandbytes
# Сейчас gradio и fastapi требуют старую версию pydantic (<2.0),
# а wandb, transformers и diffusers требуют новую версию pydantic (>=2.0).


# Мы намеренно не используем jax, flax, dopamine-rl, optax, chex, т.к. они для TPU или RL-обучения.
# У нас используется transformers==4.29.2, и это нормально для Stable Diffusion.
# pydantic==1.10.21 совместим с fastapi==0.94.0 — тоже хорошо.
# !pip install numpy==1.26.4 --force-reinstall

